'use client'

//when u using states, use stroke "use client", becouse next js its a server render

import React, { useState } from 'react'

export default function DashboardPage() {
  const [isAdmin, setIsAdmin] = useState([])

  return (
    <div>

<h1>DashboardPage</h1>


    </div>
  )
}

